spring.application.name=Urbelix
# Configuracion local para desarrollo con H2 persistente en disco


# Para usar MySQL en lugar de H2, descomenta estas líneas y configura tu servidor MySQL:
 spring.datasource.url=jdbc:mysql://localhost:3306/nexur_db
 spring.datasource.username=root
 spring.datasource.password=
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# URL del microservicio Python FastAPI
report.service.url=http://127.0.0.1:8000/analizar

# ===== CONFIGURACIÓN DE SPRING MAIL =====
# Para Gmail, usar: smtp.gmail.com con puerto 587
# IMPORTANTE: Usar contraseña de aplicación, no contraseña de cuenta personal
# Ver: https://support.google.com/accounts/answer/185833
# Asegúrate de reemplazar los valores de usuario y contraseña con credenciales válidas.

# Servidor SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.protocol=smtp
spring.mail.username=${SMTP_USERNAME:kvalero202@gmail.com}
spring.mail.password=${SMTP_PASSWORD:kevv pfta kzfz cqhl}
spring.mail.default-encoding=UTF-8

# Propiedades SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configuración de aplicación
app.name=Urbelix
app.url=http://localhost:8080

